# React-Native_Router_Example

In this application, I have created 2 pages :Signup and Login. 
Both contain forms with the same fields.I use router functionality of react to render from one page to another. 
